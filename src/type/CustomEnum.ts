export enum ImageFileExtEnum {
    jpg = 0,
    png, 
    gif,
    bmp, 
    tiff,
    svg,
    webp,
};


export enum ClientStateEnum {
    default, 
    remove,
    patch,
    add,
}

export enum RatingEnum {
    disable,
    general,
    sensitive,
    questionable,
    explicit,
}